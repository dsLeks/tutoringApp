/*
export default function validateInfo(values) {
    let errors = {}

    if(!values.email.trim()){
        errors.email = "SFSU E-mail address required"
    } else if (!/\S+@\S+\.\S+/.test(values.email)) {
        errors.email = 'SFSU E-mail address is invalid';
    }
    return errors;
}
*/
